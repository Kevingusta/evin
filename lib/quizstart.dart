import 'package:flutter/material.dart';
import 'main.dart';

class QuizStart extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Quiz'),
      ),
      body: Center(
        child: Text('Página do Quiz'),
      ),
    );
  }
}
